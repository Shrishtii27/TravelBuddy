{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "http://localhost:8001/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist"
}
